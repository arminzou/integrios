# Dev-convenience Prometheus config. Operators bring their own in production.
# Targets use Compose service names and in-container ports (not host-published ports):
# ingress/admin listen on 8080 (ASPNETCORE_HTTP_PORTS); the worker binds 5299 via UseUrls.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: integrios-ingress
    static_configs:
      - targets: ["ingress:8080"]

  - job_name: integrios-admin
    static_configs:
      - targets: ["admin:8080"]

  - job_name: integrios-worker
    static_configs:
      - targets: ["worker:5299"]
