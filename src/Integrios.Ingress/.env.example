# REST Client credentials for Ingress.http — copy to .env (gitignored) and fill in the token.
# $dotenv reads .env from the same directory as the .http file.
INGRESS_API_KEY=<token from createApiKey response>
