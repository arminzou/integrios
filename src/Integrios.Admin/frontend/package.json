{
  "name": "integrios-admin-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write",
    "generate:api": "node scripts/generate-client.mjs",
    "dev": "vite",
    "build": "biome ci && npm run generate:api && tsc --noEmit && vitest run && vite build",
    "build:assets": "npm run generate:api && tsc --noEmit && vite build",
    "check": "biome ci && npm run generate:api && tsc --noEmit && vitest run",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {
    "openapi-fetch": "0.13.5",
    "react": "19.1.1",
    "react-dom": "19.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@testing-library/dom": "10.4.1",
    "@testing-library/react": "16.3.0",
    "@types/node": "24.3.0",
    "@types/react": "19.1.9",
    "@types/react-dom": "19.1.7",
    "@vitejs/plugin-react": "4.7.0",
    "axe-core": "4.11.1",
    "jsdom": "26.1.0",
    "openapi-typescript": "7.9.1",
    "playwright": "1.62.1",
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  }
}
