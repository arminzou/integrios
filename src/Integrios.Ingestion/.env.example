# REST Client credentials for Ingestion.http — copy to .env (gitignored) and fill in the token.
# $dotenv reads .env from the same directory as the .http file.
INGESTION_TENANT_API_KEY=<token from createTenantApiKey response>
