name: ci

on:
  pull_request:
    paths-ignore: ['**.md', 'docs/**', 'LICENSE']
  push:
    branches: [main]
    tags: ['v*']
  schedule:
    - cron: '17 3 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Set up .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: '**/packages.lock.json'

      - name: Restore
        run: dotnet restore Integrios.slnx --locked-mode

      - name: Audit runtime dependencies
        run: dotnet list Integrios.slnx package --vulnerable --include-transitive --no-restore

      - name: Build
        run: dotnet build Integrios.slnx --configuration Release --no-restore

      - name: Test architecture, component, and Postgres tiers
        run: |
          dotnet test tests/Integrios.Architecture.Tests/Integrios.Architecture.Tests.csproj --configuration Release --no-build
          dotnet test tests/Integrios.Ingress.Tests/Integrios.Ingress.Tests.csproj --configuration Release --no-build
          dotnet test tests/Integrios.Admin.Tests/Integrios.Admin.Tests.csproj --configuration Release --no-build
          dotnet test tests/Integrios.Worker.Tests/Integrios.Worker.Tests.csproj --configuration Release --no-build
          dotnet test tests/Integrios.IntegrationTests/Integrios.IntegrationTests.csproj --configuration Release --no-build

  qualify:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Set up .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: '**/packages.lock.json'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Restore
        run: dotnet restore Integrios.slnx --locked-mode

      - name: Select qualification tier
        id: tier
        shell: bash
        run: |
          # Tiers select on the Tier trait the tests carry, never on test class names:
          #   database - Flyway and Postgres only, no service images, cheap enough to gate every PR
          #   smoke    - packaged deployment, fast enough for every merge to main
          #   deep     - packaged deployment, slow or timing-sensitive
          # TierTraitContractTests fails when a test declares no tier, so a new test is covered
          # by construction and this selector does not change when tests are added.
          if [[ "$GITHUB_EVENT_NAME" == 'schedule' ]]; then
            echo 'name=nightly' >> "$GITHUB_OUTPUT"
            echo 'target=tests/Integrios.QualificationTests/Integrios.QualificationTests.csproj' >> "$GITHUB_OUTPUT"
            echo 'filter=' >> "$GITHUB_OUTPUT"
          elif [[ "$GITHUB_EVENT_NAME" == 'workflow_dispatch' || "$GITHUB_REF" == refs/tags/v* ]]; then
            echo 'name=release' >> "$GITHUB_OUTPUT"
            echo 'target=Integrios.slnx' >> "$GITHUB_OUTPUT"
            echo 'filter=' >> "$GITHUB_OUTPUT"
          elif [[ "$GITHUB_EVENT_NAME" == 'pull_request' ]]; then
            echo 'name=database' >> "$GITHUB_OUTPUT"
            echo 'target=tests/Integrios.QualificationTests/Integrios.QualificationTests.csproj' >> "$GITHUB_OUTPUT"
            echo 'filter=Tier=database' >> "$GITHUB_OUTPUT"
          else
            echo 'name=main' >> "$GITHUB_OUTPUT"
            echo 'target=tests/Integrios.QualificationTests/Integrios.QualificationTests.csproj' >> "$GITHUB_OUTPUT"
            echo 'filter=Tier=database|Tier=smoke' >> "$GITHUB_OUTPUT"
          fi

      # The deployment reference defaults to a version so a fresh clone deploys without editing
      # anything. That default is only trustworthy if it cannot fall behind, so a release may not
      # be cut while it disagrees with the tag being published. Bump it, commit, then tag.
      - name: Check the deployment default matches the release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME#v}"
          defaults=$(grep -oP 'INTEGRIOS_VERSION:-\K[^}]+' deploy/compose.yml | sort -u)
          if [[ "$(wc -l <<<"$defaults")" -ne 1 || "$defaults" != "$tag" ]]; then
            echo "deploy/compose.yml defaults to [${defaults//$'\n'/, }] but this release is ${tag}." >&2
            echo "Update the default in deploy/compose.yml, commit, and re-tag." >&2
            exit 1
          fi
          echo "deployment default ${defaults} matches release ${tag}"

      - name: Record release inputs
        if: steps.tier.outputs.name == 'release'
        shell: bash
        run: |
          mkdir -p evidence/test-results
          dotnet --info > evidence/dotnet-info.txt
          docker version > evidence/docker-version.txt
          docker compose version > evidence/docker-compose-version.txt
          dotnet list Integrios.slnx package --include-transitive --format json --no-restore > evidence/dependencies.json
          dotnet list Integrios.slnx package --vulnerable --include-transitive --no-restore > evidence/vulnerability-audit.txt
          cp global.json evidence/global.json
          cp .github/workflows/ci.yml evidence/ci-workflow.yml
          cat > evidence/commands.txt <<'EOF'
          # Architecture/component/Postgres tests below ran in this workflow run's verify job; see its log.
          # The full-suite command below ran in this qualify job; see qualification.log.
          dotnet restore Integrios.slnx --locked-mode
          dotnet list Integrios.slnx package --vulnerable --include-transitive --no-restore
          dotnet build Integrios.slnx --configuration Release --no-restore
          dotnet test tests/Integrios.Architecture.Tests/Integrios.Architecture.Tests.csproj --configuration Release --no-build
          dotnet test tests/Integrios.Ingress.Tests/Integrios.Ingress.Tests.csproj --configuration Release --no-build
          dotnet test tests/Integrios.Admin.Tests/Integrios.Admin.Tests.csproj --configuration Release --no-build
          dotnet test tests/Integrios.Worker.Tests/Integrios.Worker.Tests.csproj --configuration Release --no-build
          dotnet test tests/Integrios.IntegrationTests/Integrios.IntegrationTests.csproj --configuration Release --no-build
          dotnet test Integrios.slnx --configuration Release --no-restore
          EOF

      - name: Run selected qualification tier
        shell: bash
        env:
          TIER: ${{ steps.tier.outputs.name }}
          TARGET: ${{ steps.tier.outputs.target }}
          FILTER: ${{ steps.tier.outputs.filter }}
        run: |
          set -o pipefail
          mkdir -p evidence/test-results
          command=(dotnet test "$TARGET" --configuration Release --no-restore --logger trx --results-directory evidence/test-results)
          if [[ -n "$FILTER" ]]; then
            command+=(--filter "$FILTER")
          fi

          printf '%q ' "${command[@]}" > evidence/selected-command.txt
          printf '\n' >> evidence/selected-command.txt
          "${command[@]}" 2>&1 | tee evidence/qualification.log

          python3 - "$TIER" <<'PY'
          import glob
          import sys
          import xml.etree.ElementTree as ET

          # Expected test counts are deliberately not asserted here. A hand-maintained floor
          # decays every time a test is added, and the invariant it was guarding - that no test
          # escapes tier selection - is now enforced by TierTraitContractTests. What remains is
          # a presence check that catches a filter matching nothing.
          tier = sys.argv[1]
          total = 0
          not_executed = 0
          qualification_results = 0
          files = glob.glob('evidence/test-results/**/*.trx', recursive=True)
          for path in files:
              root = ET.parse(path).getroot()
              counters = root.find('.//{*}Counters')
              if counters is not None:
                  total += int(counters.attrib.get('total', '0'))
                  not_executed += int(counters.attrib.get('notExecuted', '0'))

              qualification_ids = {
                  test.attrib['id']
                  for test in root.findall('.//{*}UnitTest')
                  if test.attrib.get('storage', '').replace('\\', '/').lower().endswith('/integrios.qualificationtests.dll')
              }
              qualification_results += sum(
                  1
                  for result in root.findall('.//{*}UnitTestResult')
                  if result.attrib.get('testId') in qualification_ids
              )

          with open('evidence/test-counts.txt', 'w', encoding='utf-8') as output:
              output.write(
                  f'tier={tier}\n'
                  f'trx_files={len(files)}\n'
                  f'total_tests={total}\n'
                  f'qualification_tests={qualification_results}\n'
                  f'not_executed={not_executed}\n'
              )

          if not files or total < 1:
              raise SystemExit(f'{tier} qualification selected no tests; the tier filter matched nothing')
          if qualification_results < 1:
              raise SystemExit(
                  f'{tier} qualification executed no tests from the qualification project; '
                  'the tier filter matched nothing'
              )
          if not_executed:
              raise SystemExit(f'{tier} qualification reported {not_executed} skipped or unexecuted tests')
          PY

      - name: Record resolved external image digests
        if: always() && steps.tier.outputs.name == 'release'
        shell: bash
        run: |
          mapfile -t images < <(
            {
              awk 'toupper($1) == "FROM" { print $2 }' docker/*.Dockerfile
              awk '$1 == "image:" { print $2 }' \
                compose.yml \
                deploy/compose.yml \
                tests/Integrios.QualificationTests/compose.deploy.qualification.yml
            } | grep -v '\${' | sort -u
          )

          : > evidence/external-image-digests.txt
          for image in "${images[@]}"; do
            digest=$(docker buildx imagetools inspect --raw "$image" | sha256sum | cut -d' ' -f1)
            printf '%s@sha256:%s\n' "$image" "$digest" >> evidence/external-image-digests.txt
          done

      - name: Upload qualification evidence
        if: always() && steps.tier.outputs.name == 'release'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: release-qualification-${{ github.run_id }}
          path: evidence
          if-no-files-found: error
          overwrite: true
          retention-days: 90

  package:
    needs: [verify, qualify]
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        service:
          # One Dockerfile builds every image; the host is selected by build argument, so each
          # published image still contains only its own service. Keep these arguments in step
          # with the same services in compose.yml.
          - name: ingress
            project: Integrios.Ingress
            build-args: ''
          - name: admin
            project: Integrios.Admin
            build-args: ''
          - name: worker
            project: Integrios.Worker
            build-args: |
              MOUNT_ROOT=/run/secrets/integrios
              HTTP_PORT=5299
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Log in to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Derive image metadata
        id: meta
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051 # v5.10.0
        with:
          images: ghcr.io/${{ github.repository }}/${{ matrix.service.name }}
          tags: |
            type=sha
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push ${{ matrix.service.name }}
        id: build
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        with:
          context: .
          file: docker/Dockerfile
          build-args: |
            PROJECT=${{ matrix.service.project }}
            ${{ matrix.service.build-args }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          sbom: true
          provenance: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Record published image digest
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
        shell: bash
        env:
          SERVICE: ${{ matrix.service.name }}
          IMAGE: ghcr.io/${{ github.repository }}/${{ matrix.service.name }}
          TAGS: ${{ steps.meta.outputs.tags }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          mkdir -p image-digest
          {
            printf 'service=%s\n' "$SERVICE"
            printf 'image=%s\n' "$IMAGE"
            printf 'tags=%s\n' "$TAGS"
            printf 'digest=%s\n' "$DIGEST"
          } > "image-digest/$SERVICE.txt"

      - name: Upload image digest
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: release-image-digest-${{ matrix.service.name }}-${{ github.run_id }}
          path: image-digest
          if-no-files-found: error
          overwrite: true
          retention-days: 90

  retain-release-evidence:
    needs: [qualify, package]
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    steps:
      - name: Download qualification evidence
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: release-qualification-${{ github.run_id }}
          path: release-evidence

      - name: Download image digests
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: release-image-digest-*-${{ github.run_id }}
          path: release-evidence/image-digests
          merge-multiple: true

      - name: Record workflow identity
        shell: bash
        run: |
          {
            printf 'repository=%s\n' "$GITHUB_REPOSITORY"
            printf 'ref=%s\n' "$GITHUB_REF"
            printf 'commit=%s\n' "$GITHUB_SHA"
            printf 'event=%s\n' "$GITHUB_EVENT_NAME"
            printf 'run_id=%s\n' "$GITHUB_RUN_ID"
            printf 'run_attempt=%s\n' "$GITHUB_RUN_ATTEMPT"
            printf 'run_url=https://github.com/%s/actions/runs/%s\n' "$GITHUB_REPOSITORY" "$GITHUB_RUN_ID"
          } > release-evidence/manifest.txt

      - name: Upload retained release evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: release-evidence-${{ github.run_id }}
          path: release-evidence
          if-no-files-found: error
          overwrite: true
          retention-days: 90
